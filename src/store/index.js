import Vue from 'vue'
import Vuex from 'vuex'

Vue.use(Vuex)

export default new Vuex.Store({
  state: {
    // levposobj: {}
  },
  mutations: {
    /** 
     * 改变分类列表的记录
    */
    // changelevpos(state, payload){
    //   Vue.set(state.levposobj, payload.key, payload.value)
    // }
  },
  actions: {
    // levpos_a(context, payload){
    //   context.commit('changelevpos',payload)
    // }
  },
  modules: {
  }
})
